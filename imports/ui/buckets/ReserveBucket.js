//  is a list of buckets selectable by the name of the event
//  can only have 5 reserve buckets at a time - if a user wishes to remove an event, they must delete a bucket
//  contains a combo box that will contain the list of reserve buckets
//  the combo box only displays if more than one reserve bucket exists
//  otherwise the reserve bucket will display the same as the current bucket with some minor differences
//  In the status section, should be marked as reserve and have all items grayed out

import BucketView from './BucketView';

class ReserveBucket extends BucketView {

}